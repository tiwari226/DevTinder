name: Keep Render Alive

on:
  schedule:
    - cron: "*/15 * * * *"  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render App
        run: curl -s --retry 3 https://devtinder-eb1f.onrender.com/

